name: CI

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: -Dwarnings

jobs:
  rust:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy,rustfmt
      - uses: Swatinem/rust-cache@v2
      - name: Check plugin and CLI contracts match
        run: |
          node -e '
            const fs = require("fs");
            const childProcess = require("child_process");
            const model = require("./Model.js");
            const metadata = JSON.parse(childProcess.execFileSync(
              "cargo", ["metadata", "--no-deps", "--format-version", "1"],
              { encoding: "utf8" }
            ));
            const cli = metadata.packages.find(pkg => pkg.name === "pidjezdy");
            const plugin = JSON.parse(fs.readFileSync("manifest.json", "utf8"));
            if (!cli || plugin.version !== cli.version) {
              console.error(`plugin version ${plugin.version} does not match CLI version ${cli && cli.version}`);
              process.exit(1);
            }
            const core = fs.readFileSync("crates/pidjezdy-core/src/config.rs", "utf8");
            const displayLimit = core.match(
              /pub\s+const\s+MAX_DISPLAY_DEPARTURES\s*:\s*usize\s*=\s*(\d+)\s*;/
            );
            const manifestLimit = plugin.barWidget.schema.find(setting => setting.key === "limit");
            if (!displayLimit
                || model.maxDisplayDepartures !== Number(displayLimit[1])
                || !manifestLimit
                || manifestLimit.max !== Number(displayLimit[1])) {
              console.error("plugin departure limits do not match MAX_DISPLAY_DEPARTURES");
              process.exit(1);
            }
          '
      - run: cargo fmt --check
      - run: cargo clippy --workspace --all-targets --locked -- -D warnings
      - run: cargo test --workspace --locked
      - run: bash tests/install-completions.test.sh

  plugin-model:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: node --check Model.js
      - run: node --test tests/model.test.js tests/workflow.test.js
      - run: node -e 'JSON.parse(require("fs").readFileSync("manifest.json", "utf8"))'

  rust-portability:
    name: Rust (${{ matrix.name }})
    # Linux is already covered by the `rust` job on every pull request. These
    # run only on the release pull request, or on demand. The branch prefix
    # alone is not enough: any pull request, including one from a fork, can
    # choose its own head branch name, so the author has to be trusted too.
    #
    # That author is whoever Release Please authenticated as, not Release
    # Please itself. The default GITHUB_TOKEN opens the pull request as
    # github-actions[bot]; a personal access token would open it as the token
    # owner instead. Both are accepted so that changing the token does not
    # silently drop release coverage. A fork can forge neither.
    #
    # Failing closed here is silent: the matrix simply would not run and the
    # release would be tagged on Linux evidence alone. If a GitHub App token
    # is ever used, add its <app-slug>[bot] login here and to the fixtures in
    # tests/workflow.test.js.
    if: >-
      (github.event_name == 'pull_request' &&
       (github.event.pull_request.user.login == 'github-actions[bot]' ||
        github.event.pull_request.user.login == github.repository_owner) &&
       startsWith(github.head_ref, 'release-please--')) ||
      github.event_name == 'workflow_dispatch'
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Windows x86_64
            runner: windows-latest
          - name: macOS Apple Silicon
            runner: macos-15
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo test --workspace --locked

  release-please:
    needs: [rust, plugin-model]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: googleapis/release-please-action@v5
